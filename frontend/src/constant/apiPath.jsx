const BASE_URL = "http://localhost:5555"

const API_PATHS = {
    book: `${BASE_URL}/api`,
    auth: `${BASE_URL}/api`,
    cart: `${BASE_URL}/api`,
    stat: `${BASE_URL}/api`,
    order: `${BASE_URL}/api`,
    img: BASE_URL
};

export default API_PATHS;